// Placeholder DB config - use Prisma or your ORM of choice
// Example: export DATABASE_URL from env and initialize client in other modules

module.exports = {
  url: process.env.DATABASE_URL || ''
};
